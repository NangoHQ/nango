{
    "name": "webhook-settings-migration",
    "version": "1.0.0",
    "description": "",
    "type": "module",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc -b tsconfig.json .",
        "migrate": "npm run build && node ./dist/migrate.js"
    },
    "author": "",
    "dependencies": {
        "@nangohq/database": "file:./packages/database",
        "dotenv": "^16.0.3",
        "knex": "^3.1.0"
    }
}
