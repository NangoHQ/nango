# Temporary — delete before merge

This file exists only to make PR #7084 match the `packages/design-system/**` path
filter in `.github/workflows/preview.yml`, so the new preview workflow actually
runs against its own PR. Revert this commit once the preview has been verified.
