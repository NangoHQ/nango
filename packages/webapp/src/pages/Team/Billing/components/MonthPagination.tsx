import { MonthStepper } from './MonthStepper';

/** In-chart drill-in month stepper (the design's "Pagination"). Backed by the shared `?month` param. */
export const MonthPagination: React.FC = () => <MonthStepper size="sm" />;
