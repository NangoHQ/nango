integrations:
    github:
        actions:
            gh-issues:
                output: Issue
                endpoint: GET /github/issue

models:
    Contact:
        id: string
    Issue:
        id: string
