integrations:
    github:
        actions:
            issues:
                output: Issue
                endpoint: GET /github/issue

models:
    Contact:
        id: string
    Issue:
        id: string
