integrations:
    github:
        actions:
            issues:
                output: Issue
                endpoint: /github/issue

models:
    Contact:
        id: string
    Issue:
        id: string
