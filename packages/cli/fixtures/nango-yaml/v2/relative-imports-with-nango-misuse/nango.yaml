integrations:
    github:
        actions:
            gh-issues:
                output: Issue
                endpoint: /github/issue

models:
    Contact:
        id: string
    Issue:
        id: string
