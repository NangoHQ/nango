import { NangoSync, NangoHelper } from '@nangohq/node';
import { <%= modelType %> } from '@nangohq/models';

class <%= syncName %> extends NangoSync {
  override async fetchData(nango: NangoHelper): { [<%= modelType %>.name]: [<%= modelType %>.type] } {
    const providerConfigKey = '<%= providerConfigKey %>';
    // Rest of the integration code goes here.
  }
}

export default <%= syncName %>;
