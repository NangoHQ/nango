import type { NangoAction<%= modelNames ? ', ' + modelNames : '' %> } from '<%= interfacePath %><%= interfaceFileName %>';

export default async function runAction(nango: NangoAction<%= input ? ', input: ' + input : ', _input?: void' %>): Promise<<%= output ? output : 'void' %>> {
    // Integration code goes here
}
