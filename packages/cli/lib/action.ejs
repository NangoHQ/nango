import type { NangoSync, <%= interfaceNames.join(', ') %> } from './<%= interfaceFileName %>';

export default async function runAction(nango: NangoSync, input?: object): Promise<{<% for (let i = 0; i < mappings.length; i++) { %><%= mappings[i].name %>: <%= mappings[i].type %>[]<% if (i !== mappings.length - 1) { %>, <% } %><% } %>}> {
    // Integration code goes here, be sure to follow the return signature type
    // and return data at the end of the action
}
