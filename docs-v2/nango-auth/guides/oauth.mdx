---
title: 'OAuth'
sidebarTitle: 'OAuth'
description: 'Learn how to let your users authorize any API with OAuth in minutes.'
---

Step 0: Run Nango with the [Quickstart](/quickstart).

## Step 1: Configure a new Integration

On the dashboard's `Integrations` tab, click the `Add New` button.

Select an pre-configured [Integration Template](/core-concepts#templates) in the dropdown or [contribute a new one](/contribute).

Fill out your OAuth app details and click `Save`.

## Step 2: Get user authorization

On the dashboard's `Integrations` tab, click the `OAuth` button for the [Integration](/core-concepts#integrations) you just configured.

Embed the generated code snippet in your frontend or provide authorization yourself by clicking the `Authorize` button.

## Step 3: Retrieve the access token

Use our [SDKs](/sdks) and [API](/api) to retrieve the access credentials by fetching the relevant [Connection](/core-concepts#connections), using the same `Connection ID` that you specified in Step 2.

Et voilà ! This is all it takes to access OAuth APIs, on behalf of users.

## Need help?

Ask any question on the [Slack community](https://nango.dev/slack).
