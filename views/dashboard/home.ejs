<!DOCTYPE html>
<html>
  <%- include('layout-head') %>
  <body>
    <!-- Header -->
    <%- include('layout-header', { req }); %>

    <main class="container">
      <section>
        <h2 class="section-heading">Enabled API</h2>
        <ul class="api-list">
          <li>
            <a href="/dashboard/all">
              <figure>
                <div><img alt="Add a new API" src="" /></div>
                <figcaption>Add a new API</figcaption>
              </figure>
            </a>
          </li>
          <% req.data.enabled.forEach(function(api){ %>
          <li>
            <a href="/dashboard/<%= api.id %>">
              <figure>
                <div>
                  <% if (api.image) { %> <img alt="<%= api.name.substr('"', '\"') %> logo" src="<%= api.image %>" /><%
                  }%>
                </div>
                <figcaption><%= api.name %></figcaption>
              </figure>
            </a>
          </li>
          <% }); %>
        </ul>
      </section>
    </main>

    <!-- Footer -->
    <%- include('layout-footer') -%>
  </body>
</html>
