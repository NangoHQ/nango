integrations:
    discourse:
        syncs:
            users:
                description: |
                    Fetches a list of active users from Discourse.
                endpoint: /discourse/users
                sync_type: full
                runs: every 1 hour
                output: User
models:
    User:
        id: number
        username: string
        name: string
        active: boolean
        admin: boolean
