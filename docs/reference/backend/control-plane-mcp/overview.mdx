---
title: 'Control Plane MCP overview (Beta)'
sidebarTitle: 'Overview'
description: 'Manage a Nango environment from an MCP client.'
---

<Warning>
    The Control Plane MCP server is in Beta because it currently uses API key authentication, which requires giving the MCP client access to a Nango API key.
    We plan to change the authentication method to be OAuth-based in a future release.
</Warning>

The Control Plane MCP server lets MCP-compatible clients manage Nango through tools. It is an agent-friendly alternative to the [public API](/reference/backend/http-api/authentication).

The Beta currently includes tools for [querying Nango logs](/reference/backend/control-plane-mcp/logs). More tools will be added over time, with the goal of supporting all public API functionality.

## Connect

The server uses the Streamable HTTP transport at:

```text
https://mcp.nango.dev/mcp
```

Configure your MCP client to send a Nango API key as a Bearer token:

| Header          | Value                    |
| --------------- | ------------------------ |
| `Authorization` | `Bearer <NANGO-API-KEY>` |

Manage API keys in **Environment Settings > API Keys** in the Nango UI. The API key determines which Nango environment the client can access, and its [scopes](/reference/backend/http-api/api-keys#scopes) determine which tools are available.

<Note>
    The `environment:mcp` scope is not required for Control Plane MCP tools. That scope is used by the separate [action-function MCP server](/guides/functions/tool-calling#mcp-server), which exposes enabled action functions as tools.
</Note>

Keep the API key server-side and only configure it in MCP clients you trust. Do not expose it in client-side code, logs, or URLs.
