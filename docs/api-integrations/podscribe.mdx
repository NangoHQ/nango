---
title: 'Podscribe'
sidebarTitle: 'Podscribe'
description: 'Integrate your application with the Podscribe API'
---

## 🚀 Quickstart

Connect to Podscribe with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Podscribe_.
    </Step>
    <Step title="Authorize Podscribe">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your Podscribe API key. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Podscribe API">
    Let's make your first request to the Podscribe API. Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl -X POST "https://api.nango.dev/proxy/api/public/campaign" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>" \
              -H "Content-Type: application/json" \
              -d '{
                "name": "My Campaign",
                "advertiser": "My Advertiser",
                "type": "DAI",
                "flights": [
                  {
                    "startDate": "2024-01-20T00:00:00Z",
                    "endDate": "2024-02-20T00:00:00Z",
                    "totalCost": 1000,
                    "cpm": 25,
                    "currency": "USD",
                    "expectedImpressions": 40000
                  }
                ],
                "mediaType": "podcast",
                "geo": "GLOBAL"
              }'
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.proxy({
            method: 'POST',
            endpoint: '/api/public/campaign',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>',
            data: {
                name: 'My Campaign',
                advertiser: 'My Advertiser',
                type: 'DAI',
                flights: [
                    {
                        startDate: '2024-01-20T00:00:00Z',
                        endDate: '2024-02-20T00:00:00Z',
                        totalCost: 1000,
                        cpm: 25,
                        currency: 'USD',
                        expectedImpressions: 40000
                    }
                ],
                mediaType: 'podcast',
                geo: 'GLOBAL'
            }
        });

        console.log(res.data);
        ```

        </Tab>
    </Tabs>
    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/guides/primitives/auth) to integrate Nango in your app.

        To obtain your own API key, follow the setup guide linked below.
    </Step>
</Steps>

## 📚 Podscribe integration guides

Nango-maintained guides for common use cases.

- [How do I link my Podscribe account?](/api-integrations/podscribe/connect)  
Learn how to generate your Podscribe API key and link your account

Official docs: [Podscribe API documentation](https://docs.api.podscribe.com/#/)

## 🧩 Pre-built syncs & actions for Podscribe

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/podscribe/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
