---
title: 'eFax'
sidebarTitle: 'eFax'
description: 'Integrate your application with the eFax API'
---

## Quickstart

Connect to eFax with Nango and make your first authenticated API request.

<Steps>
    <Step title="Create the integration">
    In Nango, open the **Integrations** tab, select **Configure new integration**, and choose **eFax**.
    </Step>
    <Step title="Connect your eFax account">
    Open the **Connections** tab and select **Add test connection**. Enter your eFax App ID, API Key, and User ID.
    </Step>
    <Step title="Call the eFax API">
    Replace the placeholders below with your Nango secret key, integration ID, and connection ID:
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/faxes" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>
        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const response = await nango.get({
            endpoint: '/faxes',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(response.data);
        ```
        </Tab>
    </Tabs>
    </Step>
    <Step title="Implement Nango in your app">
    Follow the [Auth implementation guide](/guides/primitives/auth) to add Nango to your app.
    </Step>
</Steps>

## eFax integration guide

- [How do I link my eFax account?](/api-integrations/efax/connect)

  Find the eFax credentials required by the Connect UI.

Official docs: [eFax API documentation](https://consensus.stoplight.io/docs/fax-services)

## Pre-built syncs and actions for eFax

Enable them in your dashboard, then extend or customize them for your use case.

import PreBuiltUseCases from "/snippets/generated/efax/PreBuiltUseCases.mdx"

<PreBuiltUseCases />
