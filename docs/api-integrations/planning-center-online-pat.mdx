---
title: 'Planning Center Online (Personal Access Token)'
sidebarTitle: 'Planning Center Online (PAT)'
description: 'Integrate your application with the Planning Center Online API using a Personal Access Token'
---

## 🚀 Quickstart

Connect to Planning Center Online (PAT) with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Planning Center Online (Personal Access Token)_.
    </Step>
    <Step title="Connect your Planning Center account">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_, then follow the [connect guide](/api-integrations/planning-center-online-pat/connect) to generate a Personal Access Token and enter your credentials. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Planning Center Online API">
    Let's make your first request to the Planning Center Online API (People API — list people). Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/people/v2/people?per_page=1" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.get({
            endpoint: '/people/v2/people',
            params: { per_page: 1 },
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>

    </Tabs>
    Or fetch credentials dynamically via the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/guides/primitives/auth) to integrate Nango in your app.
    </Step>
</Steps>

## 📚 Planning Center Online (PAT) integration guides

Nango-maintained guides for common use cases.

- [How do I link my Planning Center account?](/api-integrations/planning-center-online-pat/connect)
  Connect Planning Center Online (Personal Access Token) using Nango Connect UI

Official docs: [Planning Center API docs](https://api.planningcenteronline.com/docs), [Authentication overview](https://api.planningcenteronline.com/docs/overview/authentication)

## 🧩 Pre-built syncs & actions for Planning Center Online (PAT)

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/planning-center-online-pat/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
