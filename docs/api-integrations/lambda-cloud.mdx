---
title: 'Lambda Cloud'
sidebarTitle: 'Lambda Cloud'
description: 'Integrate your application with the Lambda Cloud API'
---

Lambda Cloud provides on-demand NVIDIA GPU instances for AI training and inference. Its API lets you launch, list, and terminate instances and manage SSH keys, filesystems, and firewall rules.

## 🚀 Quickstart

Connect to Lambda Cloud with Nango and see data flow in 2 minutes.

<Steps>
    <Step id="create-integration" title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Lambda Cloud_.
    </Step>
    <Step id="authorize-lambda-cloud" title="Connect Lambda Cloud">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your Lambda Cloud API key. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step id="call-lambda-cloud-api" title="Call the Lambda Cloud API">
    Let's make your first request to the Lambda Cloud API to list the instance types currently offered, with their specs, pricing, and regional availability. Replace the placeholders below with your [Environment API key](/reference/backend/http-api/api-keys), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/api/v1/instance-types" \
              -H "Authorization: Bearer <NANGO-API-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ apiKey: '<NANGO-API-KEY>' });

        const res = await nango.get({
            endpoint: '/api/v1/instance-types',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>
    </Tabs>

    Or fetch credentials with the [Node SDK](/reference/backend/backend-sdk/node#get-a-connection-with-credentials) or [API](/reference/backend/http-api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step id="implement-in-app" title="Implement Nango in your app">
        Follow our [quickstart](/getting-started/quickstart) to integrate Nango in your app.
    </Step>
</Steps>

## 📚 Lambda Cloud integration guides

Nango maintained guides for common use cases.

- [How do I link my account?](/api-integrations/lambda-cloud/connect)  
Learn how to generate a Lambda Cloud API key and use it to connect your account.

Official docs: [Lambda Cloud API docs](https://docs-api.lambda.ai/api/cloud)

## 🧩 Pre-built syncs & actions for Lambda Cloud

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/lambda-cloud/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
