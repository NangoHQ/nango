---
title: 'Holded (v2)'
sidebarTitle: 'Holded (v2)'
description: 'Integrate your application with the Holded API (v2)'
---

## 🚀 Quickstart

Connect to Holded with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Holded (v2)_.
    </Step>
    <Step title="Authorize Holded (v2)">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your **API Key**. See the [connect guide](/api-integrations/holded-v2/connect) for instructions on generating one. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Holded API">
    Let's make your first request to the Holded API (list invoices). Replace the placeholders below with your [Environment API key](/reference/backend/http-api/api-keys), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/v2/invoices" \
              -H "Authorization: Bearer <NANGO-API-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ apiKey: '<NANGO-API-KEY>' });

        const res = await nango.get({
            endpoint: '/v2/invoices',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>


    </Tabs>
    Or fetch credentials with the [Node SDK](/reference/backend/backend-sdk/node#get-a-connection-with-credentials) or [API](/reference/backend/http-api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [quickstart](/getting-started/quickstart) to integrate Nango in your app.
    </Step>
</Steps>

## 📚 Holded (v2) Integration Guides

Nango maintained guides for common use cases.

- [How do I link my Holded account?](/api-integrations/holded-v2/connect)  
Generate an API key from your Holded account settings and connect it to Nango

Official docs: [Holded API documentation](https://www.holded.com/developers/api-reference)

<Note>
Holded's legacy v1 API (auth via a `key` header) is available as the separate `holded` integration. Use `holded-v2` for the current Bearer-token-based API.
</Note>

## 🧩 Pre-built syncs & actions for Holded (v2)

Enable them in your dashboard. [Extend and customize](/implementation-guides/platform/functions/customize-template) to fit your needs.

import PreBuiltUseCases from "/snippets/generated/holded-v2/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
