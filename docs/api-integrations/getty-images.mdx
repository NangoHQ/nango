---
title: 'Getty Images'
sidebarTitle: 'Getty Images'
description: 'Integrate your application with the Getty Images API'
---

## 🚀 Quickstart

Connect to Getty Images with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Getty Images_.
    </Step>
    <Step title="Authorize Getty Images">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then log in to Getty Images. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Getty Images API">
    Let's make your first request to the Getty Images API. Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/v3/search/images?phrase=Kitties&fields=largest_downloads" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.get({
            endpoint: '/v3/search/images?phrase=Kitties&fields=largest_downloads',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>

    </Tabs>
    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/guides/primitives/auth) to integrate Nango in your app.

        To obtain your own production credentials, follow the setup guide linked below.
    </Step>
</Steps>

## 📚 Getty Images Integration Guides

Nango maintained guides for common use cases.

- [How to register your own Getty Images OAuth app](/api-integrations/getty-images/how-to-register-your-own-getty-images-oauth-app)  
  Register an OAuth app with Getty Images and obtain credentials to connect it to Nango

Official docs: [Getty Images API documentation](https://developers.gettyimages.com/docs)

## 🧩 Pre-built syncs & actions for Getty Images

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/getty-images/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
