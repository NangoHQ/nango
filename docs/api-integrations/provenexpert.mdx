---
title: 'ProvenExpert'
sidebarTitle: 'ProvenExpert'
description: 'Integrate your application with the ProvenExpert API'
---

## 🚀 Quickstart

Connect to ProvenExpert with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _ProvenExpert_.
    </Step>
    <Step title="Authorize ProvenExpert">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your API ID and API Key. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the ProvenExpert API">
    Make your first request to the ProvenExpert API (fetch your rating summary). Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl -X POST "https://api.nango.dev/proxy/rating/summary/get" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.post({
            endpoint: '/rating/summary/get',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>

    </Tabs>
    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow the [quickstart](/getting-started/quickstart/embed-in-your-app) to embed Nango in your app.

        To obtain your own production credentials, follow the setup guide linked below.
    </Step>
</Steps>

## 📚 ProvenExpert Integration Guides

Nango-maintained guides for common use cases.

- [How do I link my ProvenExpert account?](/api-integrations/provenexpert/connect)
  Learn how to obtain your ProvenExpert credentials and link your account

Official docs: [ProvenExpert API Documentation](https://developer.provenexpert.com/index_en.html)

## 🧩 Pre-built syncs & actions for ProvenExpert

Enable them in your dashboard. [Extend and customize](/implementation-guides/platform/functions/customize-template) to fit your needs.

import PreBuiltUseCases from "/snippets/generated/provenexpert/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
