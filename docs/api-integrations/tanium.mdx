---
title: 'Tanium'
sidebarTitle: 'Tanium'
description: 'Integrate your application with the Tanium API'
---

## 🚀 Quickstart

Connect to Tanium with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
        In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Tanium_. Nango doesn't provide a test account for Tanium. You'll need to set up your own by following the [setup guide](/api-integrations/tanium/connect). After that, add your API token and hostname in the integration settings in Nango.
    </Step>
    <Step title="Call the Tanium API">
        Let's make your first request to the Tanium API (validate the session). Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
        <Tabs>
            <Tab title="cURL">

                ```bash
                curl "https://api.nango.dev/proxy/v2/session/info" \
                  -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
                  -H "Provider-Config-Key: <INTEGRATION-ID>" \
                  -H "Connection-Id: <CONNECTION-ID>"
                ```

            </Tab>

            <Tab title="Node">

            Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

            ```typescript
            import { Nango } from '@nangohq/node';

            const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

            const res = await nango.get({
                endpoint: '/v2/session/info',
                providerConfigKey: '<INTEGRATION-ID>',
                connectionId: '<CONNECTION-ID>'
            });

            console.log(res.data);
            ```
            </Tab>

        </Tabs>
        Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

        ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/guides/primitives/auth) to integrate Nango in your app.

        To obtain your own production credentials, follow the setup guide linked below.
    </Step>
</Steps>

## 📚 Tanium integration guides

Nango-maintained guides for common use cases.

- [How do I link my Tanium account?](/api-integrations/tanium/connect)  
Learn how to generate a Tanium API token and connect your account.

Official docs: [Tanium REST API Documentation](https://developer.tanium.com/apis/api_intro)

## 🧩 Pre-built syncs & actions for Tanium

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/tanium/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
