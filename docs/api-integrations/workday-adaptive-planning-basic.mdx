---
title: 'Workday Adaptive Planning (Basic Auth) (Basic Auth)'
sidebarTitle: 'Workday Adaptive Planning (Basic Auth) (Basic Auth)'
description: 'Integrate your application with the Workday Adaptive Planning (Basic Auth) API'
---

## 🚀 Quickstart

Connect to Workday Adaptive Planning (Basic Auth) with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Workday Adaptive Planning (Basic Auth)_.
    </Step>
    <Step title="Authorize Workday Adaptive Planning (Basic Auth)">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your Workday Adaptive Planning (Basic Auth) username and password. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Workday Adaptive Planning (Basic Auth) API">
    Let's make your first request to the Workday Adaptive Planning (Basic Auth) API. Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/modeling/v1/default/sheet/availability" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.get({
            endpoint: '/modeling/v1/default/sheet/availability',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>
    </Tabs>
    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/implementation-guides/platform/auth/implement-api-auth) to integrate Nango in your app.

        To obtain credentials, follow the setup guide linked below.
    </Step>
</Steps>

## 📚 Workday Adaptive Planning (Basic Auth) Integration Guides

Nango maintained guides for common use cases.

- [How to configure your Workday Adaptive Planning (Basic Auth) credentials](/api-integrations/workday-adaptive-planning-basic/connect)  
Obtain credentials to connect Workday Adaptive Planning (Basic Auth) to Nango

Official docs: [Workday Adaptive Planning (Basic Auth) REST API](https://doc.workday.com/adaptive-planning/en-us/integration/adaptive-planning-rest-api/apa1659378018700.html)

## 🧩 Pre-built syncs & actions for Workday Adaptive Planning (Basic Auth)

Enable them in your dashboard. [Extend and customize](/implementation-guides/platform/functions/customize-template) to fit your needs.

import PreBuiltUseCases from "/snippets/generated/workday-adaptive-planning/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
