---
title: 'Clover'
sidebarTitle: 'Clover'
description: 'Integrate your application with the Clover API'
---

## 🚀 Quickstart

Connect to Clover with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Clover_.
    </Step>
    <Step title="Authorize Clover">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then log in to Clover. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Clover API">
    Let's make your first request to the Clover API. Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/v3/merchants/{mId}/orders" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.get({
            endpoint: '/v3/merchants/{mId}/orders',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>

    </Tabs>
    Replace `{mId}` with the merchant ID, which Nango automatically stores in `connectionConfig.merchant_id` after authorization. You can retrieve it via the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [quickstart](/implementation-guides/platform/auth/implement-api-auth) to integrate Nango in your app.

        To obtain your own production credentials, follow the setup guide linked below.
    </Step>
</Steps>

## 📚 Clover Integration Guides

Nango maintained guides for common use cases.

- [How do I link my account?](/api-integrations/clover/connect)  
  Connect your Clover account using the Connect UI

- [How to register your own Clover API OAuth app](/api-integrations/clover/how-to-register-your-own-clover-api-oauth-app)
  Register an OAuth app with Clover and obtain credentials to connect it to Nango

Official docs: [Clover REST API documentation](https://docs.clover.com/reference/api-reference-overview)

## 🧩 Pre-built syncs & actions for Clover

Enable them in your dashboard. [Extend and customize](/implementation-guides/platform/functions/customize-template) to fit your needs.

import PreBuiltUseCases from "/snippets/generated/clover/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
