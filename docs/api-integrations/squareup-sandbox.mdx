---
title: 'Square Sandbox'
sidebarTitle: 'Square Sandbox'
description: 'Integrate your application with the Square Sandbox API'
---

## 🚀 Quickstart

Connect to Square Sandbox with Nango and see data flow in 2 minutes.

<Steps>
     <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Square Sandbox_.
    </Step>
    <Step title="Authorize Square">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then log in to Square Sandbox. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Square Sandbox API">
    Make your first request to the Square Sandbox API (e.g. list locations). Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/v2/locations" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.get({
            endpoint: '/v2/locations',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>

    </Tabs>

    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/implementation-guides/platform/auth/implement-api-auth) to integrate Nango in your app.

        Use **_squareup-sandbox_** for development and **_squareup_** for production in Nango.
    </Step>
</Steps>

## 📚 Square Sandbox Integration Guides

- [How to register your own Square Sandbox OAuth app](/api-integrations/squareup-sandbox/how-to-register-your-own-squareup-sandbox-oauth-app)  
Register your own OAuth app and configure Square Sandbox for use in Nango

Official docs: [Square API documentation](https://developer.squareup.com/reference/square)

## 🧩 Pre-built syncs & actions for Square Sandbox

Enable them in your dashboard. [Extend and customize](/implementation-guides/platform/functions/customize-template) to fit your needs.

import PreBuiltUseCases from "/snippets/generated/squareup-sandbox/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
