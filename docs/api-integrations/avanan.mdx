---
title: 'Avanan'
sidebarTitle: 'Avanan'
description: 'Integrate your application with the Avanan API'
---

## 🚀 Quickstart

Connect to Avanan with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Avanan_.
    </Step>
     <Step title="Authorize Avanan">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your **API Region / Subdomain** (e.g. `smart-api-production-1-us` for USA), **Client ID**, and **Secret Key**. See the [connect guide](/api-integrations/avanan/connect) for the region list. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Avanan API">
    Let's make your first request to the Avanan API (fetch a list of tenants in Avanan). Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">
            ```bash
            curl "https://api.nango.dev/proxy/v1.0/msp/tenants" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```
        </Tab>
        <Tab title="Node">
        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.get({
            endpoint: '/v1.0/msp/tenants',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });
        console.log(res.data);
        ```
        </Tab>
    </Tabs>

    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/implementation-guides/platform/auth/implement-api-auth) to integrate Nango in your app.

        To obtain your own production credentials, follow the connect guide linked below.
    </Step>
</Steps>

## 📚 Avanan Integration Guides

- [How to connect Avanan (Application ID, Secret Key & region)](/api-integrations/avanan/connect)  
  Get your credentials from Avanan Support and choose your API region/subdomain.

Official docs: [Avanan API Reference Documentation](https://www.avanan.com/hubfs/MSP/documents/Smart-API-Documentation/master/Avanan_SmartAPI_Reference_Guide.pdf)

## 🧩 Pre-built syncs & actions for Avanan

Enable them in your dashboard. [Extend and customize](/implementation-guides/platform/functions/customize-template) to fit your needs.

import PreBuiltUseCases from "/snippets/generated/avanan/PreBuiltUseCases.mdx"

<PreBuiltUseCases />
