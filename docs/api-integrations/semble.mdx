---
title: 'Semble'
sidebarTitle: 'Semble'
description: 'Integrate your application with the Semble API'
---

## 🚀 Quickstart

Connect to Semble with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Semble_.
    </Step>
    <Step title="Authorize Semble">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter the Semble account's email and password. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Semble API">
    Semble exposes a single GraphQL endpoint — let's query a patient's first name and email. Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), [connection ID](https://app.nango.dev/dev/connections), and a real patient ID:

    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/graphql" \
              -X POST \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>" \
              -H "Content-Type: application/json" \
              -d '{
                "query": "query { patient(id: \"<PATIENT-ID>\") { firstName email } }"
              }'
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.post({
            endpoint: '/graphql',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>',
            data: {
                query: 'query { patient(id: "<PATIENT-ID>") { firstName email } }'
            }
        });

        console.log(res.data);
        ```
        </Tab>

    </Tabs>
    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/guides/primitives/auth) to integrate Nango in your app.

        To obtain your own production credentials, follow the connect guide linked below.
    </Step>
</Steps>

## 📚 Semble integration guides

Nango-maintained guides for common use cases.

- [How do I link my Semble account?](/api-integrations/semble/connect)
  Connect your Semble account using the Connect UI

Official docs: [Semble GraphQL Public API](https://docs.semble.io/)

## 🧩 Pre-built syncs & actions for Semble

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/semble/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
