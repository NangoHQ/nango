---
title: 'Lightfield'
sidebarTitle: 'Lightfield'
description: 'Integrate your application with the Lightfield API'
---

## 🚀 Quickstart

Connect to Lightfield with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Lightfield_.
    </Step>
    <Step title="Authorize Lightfield">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your API Key. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the Lightfield API">
    Let's make your first request to the Lightfield API by validating the connection. This endpoint is scope-agnostic, so it works with any valid Lightfield API key, including narrow-scope keys. Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl "https://api.nango.dev/proxy/v1/auth/validate" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>"
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.get({
            endpoint: '/v1/auth/validate',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>'
        });

        console.log(res.data);
        ```
        </Tab>

    </Tabs>
    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/guides/primitives/auth) to integrate Nango in your app.

        To obtain your own production credentials, follow the setup guide linked below.
    </Step>
</Steps>

## 📚 Lightfield Integration Guides

Nango-maintained guides for common use cases.

- [How do I link my Lightfield account?](/api-integrations/lightfield/connect)  
  Learn how to create a dedicated Lightfield API key and connect it to Nango

Official docs: [Lightfield API docs](https://docs.lightfield.app/)

## 🧩 Pre-built syncs & actions for Lightfield

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/lightfield/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
