---
title: 'AWS Inspector2'
sidebarTitle: 'AWS Inspector2'
description: 'Integrate your application with the AWS Inspector2 API'
---

## 🚀 Quickstart

Connect to AWS Inspector2 with Nango and see data flow in 2 minutes.

<Steps>
    <Step title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _AWS Inspector2_.
    </Step>
    <Step title="Authorize AWS Inspector2">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your AWS Access Key ID, Secret Access Key, and Region. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step title="Call the AWS Inspector2 API">
    Let's make your first request to the AWS Inspector2 API (list members). Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl -X POST "https://api.nango.dev/proxy/members/list" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>" \
              -H "Content-Type: application/json" \
              -d '{}'
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.post({
            endpoint: '/members/list',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>',
            data: {}
        });

        console.log(res.data);
        ```
        </Tab>


    </Tabs>
    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step title="Implement Nango in your app">
        Follow our [Auth implementation guide](/guides/primitives/auth#guide) to integrate Nango in your app.

        To obtain your own credentials, follow the setup guide linked below.
    </Step>
</Steps>

## 📚 AWS Inspector2 Integration Guides

Nango-maintained guides for common use cases.

- [How do I link my account?](/api-integrations/aws-inspector2/connect)  
Connect your AWS Inspector2 account using the Connect UI

Official docs: [AWS Inspector2 API Reference](https://docs.aws.amazon.com/inspector/v2/APIReference/Welcome.html)

## 🧩 Pre-built syncs & actions for AWS Inspector2

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/aws-inspector2/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
