---
title: 'Adyntel'
sidebarTitle: 'Adyntel'
description: 'Integrate your application with the Adyntel competitive ad intelligence API'
---

## 🚀 Quickstart

Connect to Adyntel with Nango and see data flow in 2 minutes.

<Steps>
    <Step id="create-integration" title="Create the integration">
    In Nango ([free signup](https://app.nango.dev)), go to [Integrations](https://app.nango.dev/dev/integrations) -> _Configure New Integration_ -> _Adyntel_.
    </Step>
    <Step id="authorize-adyntel" title="Authorize Adyntel">
    Go to [Connections](https://app.nango.dev/dev/connections) -> _Add Test Connection_ -> _Authorize_, then enter your API Key and Email. Later, you'll let your users do the same directly from your app.
    </Step>
    <Step id="call-adyntel-api" title="Call the Adyntel API">
    Let's make your first request to the Adyntel API. Replace the placeholders below with your [secret key](https://app.nango.dev/dev/environment-settings), [integration ID](https://app.nango.dev/dev/integrations), and [connection ID](https://app.nango.dev/dev/connections):
    <Tabs>
        <Tab title="cURL">

            ```bash
            curl -X POST "https://api.nango.dev/proxy/domain-keywords" \
              -H "Authorization: Bearer <NANGO-SECRET-KEY>" \
              -H "Provider-Config-Key: <INTEGRATION-ID>" \
              -H "Connection-Id: <CONNECTION-ID>" \
              -H "Content-Type: application/json" \
              -d '{"company_domain": "example.com"}'
            ```

        </Tab>

        <Tab title="Node">

        Install Nango's backend SDK with `npm i @nangohq/node`. Then run:

        ```typescript
        import { Nango } from '@nangohq/node';

        const nango = new Nango({ secretKey: '<NANGO-SECRET-KEY>' });

        const res = await nango.post({
            endpoint: '/domain-keywords',
            providerConfigKey: '<INTEGRATION-ID>',
            connectionId: '<CONNECTION-ID>',
            data: {
                company_domain: 'example.com'
            }
        });

        console.log(res.data);
        ```
        </Tab>

    </Tabs>

    Or fetch credentials with the [Node SDK](/reference/sdks/node#get-a-connection-with-credentials) or [API](/reference/api/connection/get).

    ✅ You're connected! Check the [Logs](https://app.nango.dev/dev/logs) tab in Nango to inspect requests.
    </Step>

    <Step id="implement-nango" title="Implement Nango in your app">
        Follow our [Auth implementation guide](/guides/auth/auth-guide) to integrate Nango in your app.

        To obtain your own credentials, follow the connect guide linked below.
    </Step>
</Steps>

## 📚 Adyntel integration guides

Nango maintained guides for common use cases.

- [How do I link my Adyntel account?](/api-integrations/adyntel/connect)  
Retrieve your API Key from the Adyntel dashboard and connect to Nango

Official docs: [Adyntel API docs](https://docs.adyntel.com)

## 🧩 Pre-built syncs & actions for Adyntel

Enable them in your dashboard. Extend and customize to fit your needs.

import PreBuiltUseCases from "/snippets/generated/adyntel/PreBuiltUseCases.mdx"

<PreBuiltUseCases />

---
